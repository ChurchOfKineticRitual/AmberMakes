{
  "mcpServers": {
    "wavespeed": {
      "type": "stdio",
      "command": "uvx",
      "args": ["wavespeed-mcp"],
      "env": {
        "WAVESPEED_API_KEY": "${WAVESPEED_API_KEY}"
      }
    }
  }
}
